# 2026-09-14

## 07:00-08:00 精选路线补录第四批：147 → 235 条（bcb8774，待 push）

- 昨夜队列跑 10 小时、成功 0 条：Overpass 被持续打到 IP 级限流（3 镜像全挂、
  返回 XML 错误页）。但 dry-run 期间落盘的 `.tmp/curate/<id>/geometry.json` 还在。
- **改用离线入库**：新写 `offline-ingest.mjs`，读缓存几何 + `proposed-entry.ts`
  直接写库，全程不联网，一次捞回 113 条候选中的 100 条（51 条通过闸门）。
  教训：跑批脚本必须常态化落盘中间产物，网络挂了也能收割。
- 修两个自己的闸门 bug：
  - core 标注没卡上界，放进了 11 条「几何反而更长」的路线（ratio 1.72~4.55），已剔除
  - 测试 REGION_BBOX 只框主城区，误杀重庆金佛山/长寿湖、大理客栈、昆明东川等远郊真路线；
    已按行政区范围放宽，并让入库脚本从测试文件解析 bbox（单一真源）
- 数据量到 235 条后，三个逐点遍历的完整性用例超 vitest 默认 5s，放宽到 30s
- `tests/pages/PerformancePage.test.tsx` 有 1 条失败，**与本任务无关**：该用例把本周骑行
  固定在当天 08:00，早于该时刻跑会被当成未来数据漏统计。属另一条工作流，08:00 后自动恢复。
  这条坑记进 `testing.md`，以后遇到「莫名其妙早上挂、上午又绿」先怀疑它。

## 08:00-09:00 第五批 +3 条（235 → 238，3d59824，CI 绿已上线）

- Overpass 恢复窗口只有约 1 小时（07:20-08:30），期间联网补跑 47 条只成功 5 条，
  质检后留 3 条；随后 de 镜像又无响应。**限流是硬约束，别指望一次跑完。**
- 剩余 42 条的失败原因高度一致：**锚点落在公园/湖心/景区步道**（nj-xuanwu 玄武湖、
  nj-zijin 紫金山、幕燕滨江等）。OSM 路网过滤掉 footway 后这些锚点所在分量不可达，
  放宽吸附到 5km 也没用——必须人工把锚点挪到外围道路。这是下一步要啃的点。
- 质检又剔 2 条：gz-university-city（比值 3.90）、dl-fengyu（1.56 虽在容差内，
  但 160km 申报 vs 250km 实绘，绝对偏离过大）。**容差只是下限，明显兜远路的不要留。**
- 各批提交与 push 情况：bcb8774（235 条）、3d59824（238 条），CI 均绿。
- 遗留：16 地区 238 条，距 20 条/地区还差 82 条；南京/青岛/青海湖/重庆各 11 条最缺。

## 09:50-10:00 AI 接入方案与原型（评审中，未写业务代码）

- 结论：值得接，但只接「帮用户说话」类（文案/解读），不做聊天机器人、不做数据问答（确定性计算本地更快，AI 有幻觉）。
- 架构：BYOK（用户自填 key），浏览器直连厂商，key 存本机浏览器不入库不导出；一个 OpenAI 兼容 adapter 通吃（DeepSeek/GLM/Kimi/OpenRouter/Claude/OpenAI/自定义均为预设常量表，预填 baseURL + 模型列表，用户只贴 key）。
- 数据边界：只上行聚合指标（Summary 级），GPS 轨迹点不出本机；AI 文案只是可选草稿，编辑确认后才进分享卡，不破坏确定性原则；结果按活动缓存。
- v1 范围：Share Studio 文案生成 + 详情页 AI 解读 两个场景；CORS 需动手前拿真实 key 实测（个别厂商浏览器直连默认不开），兜底留自定义代理地址（须 HTTPS）。
- 产出：`.tmp/ai-integration/prototype.html`（三页签交互原型：设置页 AI 配置 / Share Studio 文案 / 详情页解读），视觉复用 index.css 暗色 token。待用户评审后进实现。
- **评审定稿并已实现上线**（10:08-10:45，6495afe，CI 绿已发布）：用户拍板预设收敛 DeepSeek/GLM/Kimi/OpenRouter/通义千问/自定义（去掉 Claude、OpenAI 预设卡）。实现要点与踩坑：
  - Key/解读缓存**不入 Dexie settings 表**（settings 表被 exportImport 整表导出进 JSON 备份），改走独立 zustand persist 键 `cycling-ai-config` / `cycling-ai-insight-cache`，已写进 PROGRESS §5 硬约束
  - 本环境 npm 不可用照旧：tsc/eslint/vitest 全部直接 node 跑本地二进制（见 build-env.md）；`npm run check` 这次还被沙箱拦 wsl.exe，直接绕过
  - 实现期 3 条测试偏差：formatDistanceByUnit 是两位小数（42.70 不是 42.71）；parseCaptionResponse 对「模型只回一段话」应整体作正文不出标题
  - package.json 升版本必须**手工同步 package-lock.json 两处 version 字段**（本地 npm install 跑不了，CI 的 npm ci 会校验）
  - Share Studio 只有两平台（朋友圈/小红书），抖音走竖屏视频导出，AI 文案平台与其对齐，比原型里的三平台收敛

## 09:55-10:15 赛段重设计调研 + 一期/二期原型（评审中，未写业务代码）

- 现状调研（Explore 只读）：赛段代码在 `features/segments/`，核心短板——建段只能整趟首尾点、
  `SegmentEffort` 仅三字段且不落库（每次进页全量重扫）、只有用时榜无 PB/趋势/对比、匹配不校验方向。
  注意与详情页「距离分段」`features/activity/segments.ts` 是两套东西，别混。
- Strava 调研：赛段核心是自动匹配 + 活动页「本次 vs PR」差值 + My Results 散点趋势 + 成就体系
  （CR/前三/Local Legend）；推荐段长 0.4–1 km。
- 方案分期：一期 = effort 落库（segment_efforts 表）+ 活动页「本次赛段」对比区块 + 赛段详情页
  （统计/前三/趋势散点/历史表）+ 列表页信息升级；二期 = 地图框选建段（方向校验/命中预览）+
  成就体系（CR/前三/当地传奇）+ 新 PR 分享卡（复用 shareCard）。
- 产出：`.tmp/segments-redesign/segment-redesign-prototype.html`（一期 3 视图 + 二期 3 视图，
  mock 数据用时/均速/差值自洽）。待用户评审后进实现。

## 10:45-11:30 赛段重设计一期实现完成（517ef31，CI 盯控中）

- 数据层：DB v6 `segment_efforts`（`&[segmentId+activityId]` 唯一复合索引）；
  `segmentRepository` 加 `getSegment/listEffortsBySegment/replaceSegmentEfforts/
  upsertActivityEffort/deleteEffortsByActivity/markEffortsSynced`，删赛段/删活动/
  清空数据三级联删成绩；`SegmentEntity.effortsSyncedAt` 非索引字段标记「扫描过」。
- 匹配层：`matchSegmentEffortDetail` 返回最佳穿越窗口（原函数变薄壳，worker 兼容）；
  `effortMetrics.ts` 算窗口内均速（GPS 路径距离/用时）/均功率/均心率；
  `buildSegmentLeaderboard` 产出带指标 effort；`segmentStats.ts` 统一距离口径。
- UI：详情页核心指标后插 `ActivityMatchedSegments`（实时匹配 + vs PR 差值 +
  新纪录/前三/首条徽章 + 本地源回写）；新 `/segments/:id` `SegmentDetailPage`
  （统计/前三/Recharts 趋势散点金点 PR/历史表）；列表卡片加 PR/次数/最近差值，
  榜单封顶 10 条链到详情页；赛段页全量扫描结果自动落库。
- 坑：react-hooks 新规则 `set-state-in-effect` 禁止 effect 体内同步 setState——
  重置状态的 setState 要挪进 effect 内的 async IIFE 首行；测试差值文案 ≥60s
  走 `±M:SS` 格式（-200s 显示为 -3:20），断言按真实格式写。
- 测试：赛段模块 10 文件 82 例 + 全量套件全绿（fast-check PASSED）。
- 版本 2.77.0；changelog + PROGRESS.md 已同步。原型保留在
  `.tmp/segments-redesign/` 供二期（地图框选建段/成就体系/分享卡）实现时参考。

## 13:40-14:10 赛段重设计二期实现完成（版本 2.79.0）

- 地图框选建段：`segmentCreator.ts` 纯函数（nearestRecordIndex/buildSegmentDraft/
  effortMatchesDirection）+ `SegmentCreatorDialog.tsx`；详情页「设为赛段」改开弹窗，
  轨迹点两点截取任意路段，方向正反切换、0.4–1km 推荐提示、近 90 天命中预览
  （含正反分布）。坐标存原始记录坐标（与匹配器同口径，勿转换 WGS-84）。
- 成就摘要：`computeSegmentAchievements`（在 segmentStats.ts，react-refresh
  only-export-components 规则禁止组件文件导出纯函数）+ `SegmentAchievements.tsx`
  挂赛段页：纪录赛段数/前三成绩数/当地传奇（近 90 天）。
- 新纪录分享卡 `segmentPrShareCard.ts`（独立 Canvas 绘制，不塞 shareCanvas 的
  ShareData——活动维度与赛段维度数据结构差异大）；「本次 vs 最好」前后半程
  展开对比（最好成绩窗口不落库，展开时懒加载该活动逐点现算）。
- 坑：并行会话在我工作期间发布了 2.78.0（AI 供应商管理），二期改用 2.79.0——
  **提交前必查 git log 与 changelog 头部，版本号撞车会覆盖别人条目**。
- 测试：segmentCreator 6 例 / segmentAchievements 4 例 / 对比展开与分享图 2 例，
  赛段模块 89 例 + 全量套件绿。

## 14:30-16:30 三期：跑偏修复 + 本地挖掘 + AI 起名（6fbaaee / a37f3c5 / 11ee8f4）

- **用户否决 Strava 路线**（无账户）；调研确认开源世界没有现成赛段数据库
  （Trackopedia 仅提案、BikeRoutes 是路线不是赛段、OSM 只有路网）。
  定案三层：A 修 Bug → B 本地挖掘（算法打底）→ C AI 只做命名增强层
  （否决「AI 先行分析轨迹」：GPS 不上行红线 + 不可复算 + 烧钱）。
- **A（2.81.1）点击跑偏真因**：弹窗点击是底图 GCJ-02 坐标，吸附却对
  WGS-84 原始记录坐标算最近点，偏差 300~600m。修法：吸附改在投影展示
  轨迹上算（nearestProjectedIndex + 50m 拒绝半径），projected useMemo
  同时产出「投影索引→记录索引」映射。
- **B（2.82.0）segmentMining 三步算法**：0.002° 热力格（去重活动数）→
  热格对滑动窗口配对（窗口深度 4——只看相邻对会被 300m 下限全滤掉）→
  格中心做端点的候选段（端点稳定性是自动建段头号问题）+ 代表活动切片。
  去重用**中点邻近 <maxPairMeters**（初版 750m 挡不住抖动产生的嵌套变体，
  放宽到 1200m；平行街道 >1.2km 不误伤）。已知限制：格对距离直线近似，
  弯道走廊漏挖（v1 取舍）。
- **C（2.83.0）AI 起名**：buildSegmentNameRequest 只上行距离/次数
  （红线：无坐标样式数字进 prompt，测试有断言），selectAiReady 控制显隐。
- 并行会话期间已做「活动页赛段 AI 点评」（SegmentAiComment）与 2.81.0，
  **版本号撞车两次**（2.78→2.79、2.81→2.82/2.83）——提交前必查远端版本。
- 本地代理间歇 502/SSL 中断，推送多次重试；A 已上远端，B/C 挂后台重试。

## 10:45-11:15 AI 接入试用反馈两轮 → 定版 v2 方向（供应商管理照 cc-switch 重做）

- 试用问题 1（已诊断未改码）：「接口地址或模型名不存在」= cc-switch 的 baseUrl `https://openrouter.ai/api` 缺 `/v1`（本站约定以 /v1 结尾直拼 /chat/completions）；探针实测带 /v1 → 401（路径对），不带 → 404（即报错来源）。OpenRouter 预设三个 slug 与用户的 ling:free 模型都在公开 /models 列表里。
- 试用问题 2：`ling-3.0-flash-vl:free` 支持 reasoning，「连接测试」max_tokens=8 被思考 token 吃光 → 200+空 content → 误报「模型返回了空内容」。修复方案已提出（200 即算连通、上限提高、200+error body 透出真实原因、文案上限 800）待随 v2 一起实施。
- **用户拍板：现在的单配置版「基本不可用」，重做成 cc-switch 形态**——多供应商配置列表（添加/编辑/删除/一键启用切换）+ 六个预填模板 + 「获取模型列表」按钮（GET /models 填充下拉，解决手抄模型名）。原型 `.tmp/ai-integration/prototype-v2.html` 已出待评审。
- 安全：用户把完整 OpenRouter Key 贴进过对话，已提醒 revoke（实测旧 Key 401 User not found，应用里应为新 Key）。
- 暂不移植：多端点/anthropic-messages 协议/50+ 中继站预设（理由已向用户说明）。

## 11:21 v2 原型追加：预设库对齐 cc-switch + OpenAI/Claude 回归

- 用户要求预设「尽可能全」照 cc-switch：其 MIT 许可，从 codexProviderPresets.ts 抽出 76 个端点，剔除 OAuth 卡/订阅计划变体/纯 anthropic 端点后收 58 条（官方 21 + 聚合 7 + 中转 30），原型改为「常用 6 卡置顶 + 更多供应商搜索面板（分类 chips + 名称/域名搜索）」。
- 用户随后要求 OpenAI、Claude 也加回预设：已加入常用卡与官方分类。**实现要点：Claude 走其 OpenAI 兼容层（/v1），但浏览器直连必须带 anthropic-dangerous-direct-browser-access 请求头——aiClient 需支持预设级 extraHeaders（providers.ts 加字段）**，实施时一并做。

## 11:30-12:00 AI 供应商管理 v2 实现上线（939a77c，CI 绿已发布，2.78.0）

- 用户拍板「开搞」后实现并闭环。要点：
  - aiConfigStore 重构 profiles 数组 + activeProfileId；persist version 0→1，migrate 把 v1 单配置（含 Key）自动搬成 profiles——**线上已有 v1 用户的 Key 不丢**
  - addProfile 的 id 最初用裸 Date.now() 被测试抓出同毫秒撞 id（并行添加互相覆盖），补了随机段
  - 预设库拆 AI_COMMON_TEMPLATES（8）+ AI_PRESET_LIBRARY（59，cc-switch MIT 同源），extraHeaders 机制解 Claude 浏览器直连
  - 并行会话（赛段一期）在实现期间发布了 2.77.0，版本号当场顺延 2.78.0——**共享文件的版本号在提交前必须再核一遍 git log**
- 全量 1569/1569 绿；.tmp/ai-integration 已清理。

## 13:45 v3 方向：agent 式生成（思考可视化 + 终止），原型待评审

- 用户否决「关思考」方案，拍板新交互：思考过程流式展示（完成后自动折叠）→ 正文流式落框 → 全程可终止；终止即断流、按实际用量计费——用「看得见的成本」替代硬性禁止思考。
- 原型 `.tmp/ai-integration/prototype-v3.html`：思考卡（live 状态/自动折叠/手动展开）、终止按钮、耗时+token+预估费用统计。待评审后实现——**实现要点：aiClient 需新增 SSE 流式解析（stream:true，reasoning/content delta 逐段回调），AbortController 已有现成信号机制**；连接测试保持非流式。

## 13:51 v4 方向：AI 增强三大内容面 + 输出上限设置，原型待评审

- 用户要求：骑行洞察/综合评分/赛段接入 AI 后内容更丰富（不要枯燥死板），**默认输出本地确定性结论，点「AI 解读」才替换，可切回**；输出上限做成设置项，默认 10000 token（防失控调用的硬顶）。
- 原型 `.tmp/ai-integration/prototype-v4.html`：三个区块（洞察 3 条叙事化 / 评分构成解读 / 赛段逐条点评+综述）各带「AI 解读 ↔ 回到本地结论」toggle + 输出上限设置行。AI 内容风格：叙事化、连成故事、给可验证假设，禁编造（数字全部来自本地结论）。
- 实现时注意事项（待用户确认原型后开工，[NF] 2.79.0 起）：
  - v3 的 SSE 流式 + 思考卡 + 终止 与 v4 的内容面共用同一套 agent 交互
  - 输出上限存 aiConfigStore（全局设置），max_tokens = min(用户上限, 场景默认)
  - 洞察/评分/赛段的 AI 提示词需把本地规则结论（buildRideInsights/QualityScore/segments 差值）作为输入喂给模型，禁止编造
- v4 追加：「一键解读整个骑行」——洞察/评分/赛段依次生成（串行 loadSection），区块级单条解读保留。
- v4 再追加：区块主按钮改三态（AI 解读 → 回到本地结论 ↔ AI 结论）+ 独立「重新生成」按钮（每区块 mock 两版内容演示换一版）；一键解读只补未生成的区块。

## 14:02-14:11 App 端 + 码表接入调研（只做诊断，未改代码）

- 结论：不需要重构，推荐 Capacitor 包壳（一份代码发 Web/Android/iOS）+ 码表接入分 v1 文件导入 / v2 BLE 直传两阶段。
- GitHub 调研「同步各家码表」开源工具许可证（决定能否借鉴代码）：
  - GPL-3.0 不可搬代码：`L1cardo/Garmin-Cycle-Sync`（Garmin CN → 捷安特/顽鹿/iGPSPORT/行者，Python 云端 API，方向与本项目相反）。
  - MIT 可参考重写：`ekspla/xoss_sync`（BLE + Nordic UART + Ymodem 从 XOSS/Cycplus 拉 FIT）、`andrewkomkov/cycplus-m2-sync`（Kotlin，含完整协议文档，2026 活跃）、`jorge-huxley/intervalssync`（iGPSPORT/Bryton 云端 API，活跃）。
  - 无 license（默认版权保留，只看思路不抄码）：`kvnZero/IGPSPORT2Xingzhe`(81★)、`simple4wan/ride-sync`、`DCNick3/f-xoss`。
  - 国内 XOSS/Cycplus 系码表多用同一套 Nordic UART + Ymodem 协议，v2 BLE 直传可从该协议起步。
- 用户诉求：接入码表、数据一键导入本项目；方向是「手机 App 收码表数据」，平台互传类工具只作反向分发时的思路参考。
- 追加（14:18）：深查 `Anathleticbicyclist/sync-igpsport-magene-onelap-xingzhe-data-to-outbase`（迈向Ob）——MIT、Kotlin 原生、2026-08 创建日更至 v8.2.1、13 个来源平台（iGPSPORT/行者/迈金/黑鸟/百锐腾/佳明 CN+Intl/高驰 CN+Intl/Wahoo/MyWhoosh/Zwift/Keep）→ Outbase。质量不错（结构清晰/去重/WorkManager 后台同步/中英免责声明），但全是云端 API（WebView 登录抓凭证），**无 BLE 代码**。对本项目价值：v1.5「厂商云拉取导入」路线的 MIT 参考实现（api/ 目录各平台登录+FIT 下载），且该路在纯 Web 端受 CORS 限制、进 Capacitor 壳后才可行；不能替代 BLE 直传。
- 追加（14:27）：讨论「纯网页直接登第三方账户拉云端」可行性并实测 CORS：**迈金 onelap.cn API 返回 `Access-Control-Allow-Origin: *` 且允许 Authorization 头**（浏览器可直连的候选起点）；行者/iGPSPORT/佳明中国实测均无 ACAO 头（纯网页不可读响应）。分层结论：纯 API 登录 + 开放 CORS 平台可浏览器直连；纯 API 登录但无 CORS 者需 BYO 自建代理；WebView 登录型平台纯网页无解（拿不到第三方 cookie），只能 Capacitor 壳后原生做。云端拉取即便纯 Web 可行，PWA 无后台轮询能力，自动同步仍需壳后 WorkManager。架构沿用 AI 接入的本地凭证 + 可选代理哲学，每平台独立 adapter 可插拔。

## 14:44 App 端落地方案输出（`.tmp/app-capacitor/落地方案.md`，待评审）

- 方案要点：Capacitor 壳（webDir=dist，业务代码零改动）+ CapacitorHttp（一行配置消除 CORS）+ 自研 cloud-login 插件（原生 WebView 登录收凭证，复刻迈向Ob LoginWebActivity）。三形态并存：网页/PWA/APK，`isNativePlatform()` 统一降级。
- cloudSync 模块：`src/features/cloudSync/`（adapter 接口 login/list/download，产物直接喂现有导入管线）；唯一数据层变更是新表 `sync_ledger`（platform+externalId 记账）；凭证独立 persist 键不进 Dexie 导出（照搬 AI key 模式）。
- 阶段：0 壳冒烟 → 1 迈金 adapter 全链路（网页也可用）→ 2 cloud-login 插件 + WebView 登录型逐平台 → 3 WorkManager 后台同步。CI 新增独立 apk.yml（不阻塞 deploy.yml）。
- 文档含 10 项风险对策与 4 个待用户确认决策点（阶段顺序/首个 WebView 平台/APK 分发/网页入口位置）。评审通过后实施。

## 14:00-16:00 v3+v4 全部实现上线（d83d8c1 2.80.0 / d512d42 2.80.1 / ff87c77 2.81.0，CI 均绿）

- 2.80.0 agent 化：streamChatComplete（SSE reasoning/content delta，无总超时，外部终止返回部分内容不抛错，非 SSE 响应兜底整体 JSON）+ useAgentStream hook + AgentThinking 思考卡 + maxOutputTokens（默认 10000）；文案/解读迁移流式。
- **2.80.1 热修（上线当天用户踩到）**：非 SSE 响应体兜底解析只取行缓冲末行碎片 → 多行格式化 JSON 必报「流式读取中断」。重写读取循环（rawAll 全量累积 + 行缓冲逐行；未出现 data: 时整体 JSON 解析）。教训：SSE 解析的兜底路径必须用「原始全量文本」而不是被 pop 过的行缓冲。
- 2.81.0 内容面：AiEnhanceBlock（本地/AI 双态 + 重新生成 + 缓存）+ enhanceRegistry（模块级有序注册表，useSyncExternalStore）+ AiEnhanceAllButton（串行一键解读）+ SegmentAiComment（内嵌赛段组件尾部）+ 三个改写类提示词（NARRATIVE_CONSTRAINTS 禁编造，只喂本地结论）。
- 工程坑：react-hooks 新规则禁 effect 内 setState（改 onFinish 回调 / 派生状态）；render 期禁 ref 写入（改 effect 同步）；addProfile 裸 Date.now() 同毫秒撞 id（补随机段）；**lockfile 版本字段链式 replace 两次没命中静默落后（2.78.0），发布脚本必须以 package.json 为准整体写 lock**。
- 全量 1603/1603 绿；.tmp/ai-integration 已清理；赛段二期并行会话两次顶掉版本号（2.77.0/2.79.0），本批次实际发布 2.80.0/2.80.1/2.81.0。

## 15:57 v5 UI 重设计原型（针对「加了 AI 后页面乱」）

- 用户反馈 AI 解读文字效果不好、页面乱，要求 UI 优化、先出原型。原型 `.tmp/ai-integration/prototype-v5.html`，四个设计决策：
  1. 入口收敛：每区块一个「✦ AI 解读」药丸（青色 #7ee0c3 与功能绿区分），生成后变 [本地|AI] 迷你分段 + ⟳ 小圆钮
  2. AI 内容独立版式：左渐变竖条 + 淡青底卡（ai-block），元信息行（AI 生成 · 思考 token · 耗时），正文支持加粗高亮
  3. 思考过程降级为一行（生成中 shimmer 骨架 + 思考中状态行，完成后「思考 · N token」可展开回看），不再用大思考卡
  4. 一键解读从大卡片缩成页头小药丸
- 待评审后实现（[NF] 2.82.0 前后）：AiEnhanceBlock/AgentThinking 改版式即可，流式底座不动。

## 16:29-16:40 v5 提示词改造上线（175ee1e 2.82.0，CI 绿）

- 针对用户「AI 内容与本地版大差不差」：根因是只喂结论+禁推算，模型只能润色。改为喂原料派任务——AiRichFacts（buildSplits 5km 分段/心率、buildClimbs 爬坡、recentBaseline 文本）+ 视角轮换（pacing/body/history，insightVariantRef 驱动）+ 新约束（数值来自所喂数据、至少 2 条本地结论外的新观察、禁逐条复述）。
- 注意：CI 有一个 changelogPage.test 的既有告警（build: tests/pages/changelogPage.test.tsx#41）但整体绿。
- v5 UI 改版原型（prototype-v5.html）用户尚未拍板，待确认后做 2.83.0 版式层改造。
