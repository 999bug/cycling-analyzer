# 2026-09-04 工作日志

## 审计修复复查（用户按 2026-09-01 报告自行优化，我方复查）

用户完成了 8 个提交（077ebfd→cbe8cf2）+ 版本提交 6234c10（2.29.0）。复查结论：**修复质量高**，974/974 测试全绿 + lint 干净。

### 已验证合格的修复
- maxOf 循环化（超栈根因消除）、坐标 ?? 0 → 类型谓词拒绝建段
- ErrorBoundary（实现规范，含 aria-live/reload/英文日志）
- parseClient WorkerParserHandle（60s 超时 + terminate + rejectAll + dispose）
- db.ts blocked/versionchange、QuotaExceededError 分类
- 赛段页批量协议（LeaderboardBatchRequest + getRecordsByActivityIds + 一次 postMessage + 主线程同步回退）
- 爬升 totalAscent 优先 + 累加回退；avgCadence 排滑行 + NP 满窗
- localDateKey/formatDurationText 收敛 utils/format.ts；shareCard 改用 METERS_PER_MILE
- 71f5bb2：实测排除时区假设（两失败用例是断言跨月边界漏洞，过滤逻辑走本地 Date 毫秒），startsWith 真 bug 诚实记为遗留待专项

### 复查新发现（小问题，待修）
1. `db.ts:329` `cycling-db-blocked` 自定义事件是死代码：全仓无监听方，且 window.dispatchEvent 无法跨标签触发（浏览器本会自动对旧连接触发 versionchange）——删 dispatch 或改 BroadcastChannel
2. `parseClient.ts` dispose 注释前后矛盾（"不应误伤"vs"一并拒绝"），以代码为准改注释
3. `calculator.ts` 仍 6 个 `.map()` 中间数组（超栈已解，仅余 GC 小尾巴，可合并单次遍历）
4. 审计报告 md 仍 untracked，应 [DOC] 入库

### 报告遗留未完成项
- **P0 唯一遗留：exportImport.ts exportData 全量入内存（备份通道 OOM 风险）**
- P1：buildClimbs 仍 3 次/渲染（ActivityDetailPage:405/SegmentsSection:356/qualityScore:153/rideInsights:317）；首屏 recharts 412kB 仍 modulepreload（App.tsx:7 静态 DashboardPage + AppLayout→ImportPanel 静态链）；activityRepository startsWith UTC 真 bug 待专项
- P2：ESLint no-restricted-imports 边界强制、SettingsPage 718 行拆分、ROUTES 下标引用、Recharts 类名断言×15、LoadState×6、activities 搜索防抖

## 复查修复批落地（15:44 会话）

新增提交（已推送/远端已同步）：
- `3861b43` [BF] 复查修复批：calculator `calculateSummary` 单遍聚合（删 maxOf/averageOf/averageExcludingZero，消 7 个 n 长中间数组）；db.ts 删死事件 `cycling-db-blocked` + versionchange 先 `this.close()` 放行；parseClient dispose 注释修正。版本 2.29.1
- `31a2f47` [DOC] 审计报告 md 入库。版本 2.29.2
- 验证：974/974 + lint + tsc 全绿
- ⚠️ 环境怪象：`.git/refs/remotes/origin/*` 会被反复清空（fetch 报告写入后随即消失，疑似 IDE git 工具行为），不影响 push（git 直连远端比对），仅影响 status 的 ahead/behind 显示
- 下一批建议顺序：exportData 分片导出（P0 遗留）→ 首屏体积（recharts/importer 懒加载）→ startsWith UTC 专项
