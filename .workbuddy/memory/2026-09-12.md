# 2026-09-12

## 分享素材 v2 推送 + CI 时区事故修复（2.74.1）

- **推送**：GitHub HTTPS 在本环境仍走不通（代理 502 / SSL_ERROR_SYSCALL，绕代理直连超时），
  但 **SSH 通**——改用显式地址 `git push git@github.com:999bug/cycling-analyzer.git main:main`
  一次成功，一期 `dab3610` + 二期 `690b8ee` 都已上线（`875589b..690b8ee`）。已记入 `build-env.md`。
- **CI 红**：`gh run list / view --log-failed` 可见 2 个用例挂（`ShareStageCard` 日期文案、
  `ShareStudioModal` 下载文件名）。根因 = 样例时间 `2026-09-06T07:30:00+08:00` 在 CI（UTC）
  落到前一天 → 按本地时区渲染/取文件名就跨日了（本地 +08:00 看不出来）。
- **修复**：三个分享测试的样例 `startTime` 改**不带时区偏移**（`'2026-09-06T12:00:00'`，
  按本地时间解析，任何时区同一天）；本地 `TZ=UTC vitest` 复现 → 修复后双时区全绿（1486/1486）。
- 版本 2.74.1（changelog 顶部条目同步改号；2.74.0 因 CI 未部署、未对用户生效，不单独留条目）。
- 经验已写进 `testing.md`：测试样例日期时间不带偏移 + 推送前用 `TZ=UTC` 自检。

## 类型复核收敛：入口合并 + 修正后不再提示（[NF] 2.75.0）

- 用户反馈：勾选「修正类型」改完后，工具栏「批量修正运动类型（N）」的提示仍消不掉。根因＝检测无状态：灰区记录库里类型仍是 cycling，每轮推断都判 grey → 永远被重新检出。
- 三项改动：①删除勾选操作条「修正类型」按钮（2.72.0 引入），能力并入批量修正弹窗——新增「可疑记录 / 全部记录」范围切换（allowFullRange），全部记录配搜索 + 按当前类型 chips + 200 条渲染上限 + 默认不勾选；②应用即确认：`ActivityEntity.typeConfirmedAt`（Unix 秒，非索引字段免升版本）+ 仓储 `confirmActivityType/clearTypeConfirmations`，`detectTypeSuspects` 跳过已标记记录；③设置页「数据管理」加「重置类型提示」后悔药。
- 坑：`clearTypeConfirmations` 用 `where(':id').modify(delete 字段)` 真正移除键（update 传 undefined 会留空键）；`toActivityEntity` 是显式逐字段映射，新非索引字段**不要**加进去（新导入视为未确认）；测试夹具必须走真实 `detectTypeSuspects`（手工构造 suspects 会掩盖检出语义）。
- related 首跑 2 个偶发失败（并行执行 flake），复跑 718/718 全绿；版本 2.74.1 → 2.75.0。

## 示例数据说明条改版（[NF]，随 2.75.0 入库）

- 用户反馈首屏提示「你现在看到的是作者 Saul 的公开骑行数据…」不明显。诊断：视觉权重最低档
  （info 10% 背景/30% 边框，深色下与 --bg-surface 几乎同色）+ 80 字长句无分层 + 只有 × 没有行动入口
  + 关闭即永久消失（独立 localStorage key）。UI 专家会话出方案对比图后确认：徽章 + 双层文案 + 导入按钮。
- 落地：`AuthorBanner` 重写（「示例数据」徽章 + 主句「你现在看到的不是自己的骑行记录，而是站点内置的示例数据」
  + 次要句「导入你的 FIT 文件后…」），主色「导入我的数据」按钮；文案**不再提作者身份**（用户明确要求）。
- 两个技术点（重要）：
  - 导入弹窗开合从组件 useState 提到 `importStore.dialogOpen`，侧边栏按钮与说明条共用；
    **必须在 ImportPanel 卸载时 closeImportDialog**，否则 store 残留 true → 测试间交替失败
    （表现：第一个用例开弹窗成功、第二个点按钮变关闭，严格交替）。
  - 弹窗改 `createPortal(..., document.body)`：移动端侧边栏是 `transform: translateX(-105%)` 抽屉，
    会为 `position: fixed` 创建包含块，从横幅唤起时弹窗会跟着抽屉跑偏（原实现只在侧栏内打开，问题被掩盖）。
- 关闭状态迁入 `dataSourceStore.authorBannerDismissed`（持久化），设置页「作者数据」区块可恢复显示。
- **并行会话踩踏实录**：本会话用 `git apply --cached <只含自己 hunk 的 patch>` 精确暂存共享文件
  （SettingsPage.tsx 同时被另一会话改），但对方随后 `git commit` 把我的 index 一并提交进了
  523ed83（主题只写类型复核）。教训：并行时即使只暂存自己的改动，也挡不住对方 commit 带走 index
  ——共享文件改动最好等对方提交完再动，或提交前先确认 index 归属。文档补充走了单独提交 `e92055e`。
- **事故记录：提交卷入并行会话半成品**——git add 前查过 status 只有自己文件，但 add+commit 之间并行会话把 AuthorBanner/ImportPanel/importStore/dataSourceStore 等 6 个文件 add 进了暂存区，一并进了 523ed83。红线是「绝不改写历史」，无法拆分；补救＝跑被卷入文件的相关测试（31/31 绿）+ CI 全量验证（通过）。**教训：共享仓库里 commit 前必须再查一次 `git diff --cached --stat`，只提交自己文件时应先 `git restore --staged` 掉非本任务的暂存文件**；后续会话一律照此执行。

## 手机端全屏卡死修复（[BF] 2.75.1，f73f63f）

- 用户真机症状：PWA 安装版（standalone）点地图全屏整页冻结，只能杀页面重开；版本 2.74.1（昨天伪全屏修复已在跑）仍卡。
- **关键诊断结论**：桌面 headless 探针（Playwright 移动视口 + init 阶段 stub matchMedia 强制 standalone）验证伪全屏路径完全干净（无报错/无 resize 风暴/帧率正常）——伪全屏代码本身没有死循环。真机仍卡 ⇒ 根因指向「**standalone 判定不可靠**」：添加到主屏幕的快捷方式（非完整安装）、部分 WebView 里 `display-mode` 媒体查询不匹配 → `isStandaloneDisplay()` 判 false → 误走原生 `requestFullscreen` → 移动 Chromium 卡死 bug 族。
- 修复（A+B+C）：
  - A：`pseudoFullscreen.ts` 新增 `isMobileLikeDevice()`（`(pointer: coarse)` 或视口 ≤768px 兜底），`toggleFullscreen` 三路分支——移动端一律伪全屏，原生 API 只留给桌面；
  - B：`replayEngine.ts` emitFrame 逐监听器 try/catch（瞬态异常不杀 rAF）；`TrackReplay.tsx` ReplayOverlay 监听 window resize 复位 `panningRef`（过渡期 pan 动画被打断 moveend 不触发 → 镜头永久卡住）+ followCursor 零尺寸守卫；
  - C：`notifyResizeAfterLayout` pending 去抖；移动端回放条 `backdrop-filter: none`。
- 测试：mapFullscreen +触摸/窄视口 2 用例、replayEngine +异常兜底 1 用例；vitest 22/22 + npm run check 绿；CI 待盯（后台 gh run watch）。
- 工具坑：bash 里跑 `npx vitest` 会被沙箱拦 wsl.exe（输出乱码+阻断），**vitest 一律用 PowerShell 跑并重定向日志文件再读**；长输出先落 `.tmp/<task>/xxx.log` 再 sed 去 ANSI 提取。探针脚本在 `.tmp/fullscreen-freeze/probe.mjs`（任务结束可删）。
- 注意：`display-mode` 判定也影响 PWA 安装引导模块（install.ts），本次没动；若后续安装横幅在快捷方式环境误显示，同源问题。

## Dependabot 漏洞修复（vitest 升级，未完成暂停）

- Dependabot 两条 moderate（#5 #6）同源：@vitest/mocker 路径穿越，修复版 4.1.11；项目锁在 4.1.10。
- 进度：`package.json` 已升 `"vitest": "^4.1.11"` + 版本号 2.75.2 + changelog 条目已加；`package-lock.json` 已更新（vitest/mocker 4.1.11，23 条 resolved 从 npmmirror 翻成 npmjs——用户 ~/.npmrc 现行就是 npmjs，属顺带修正）。**均未提交**。
- 坑：bash 里 `npm`/`npx` sh 包装脚本乱码并触发 wsl.exe 沙箱拦截，一律用 `<managed-node>/npm.cmd` / `npx.cmd` 全路径。
- 坑：npm 10.9.7 arborist 在本项目报 `edgesOut of null`，`--legacy-peer-deps` 能装上但会清掉自动 peer（@testing-library/dom 全量测试挂）；后一次完整 `npm install` 也未能修复 node_modules（vitest 报 `reading 'config'`）。
- 中断点：干净重装（rm -rf node_modules && npm install）被沙箱 safe-delete fail-closed 拦下（genie-trash 127），node_modules 仍是坏状态，测试 155 文件全挂。
- 恢复步骤：① 换 PowerShell 跑 `Remove-Item -Recurse -Force node_modules` + `npm.cmd install`（或 npm ci）；② `npm.cmd run check` 全绿；③ 提交 4 个文件（package.json / package-lock.json / changelogData.ts，Subject `[IM] 升级 vitest 4.1.11 修复 Dependabot moderate 漏洞`，版本 2.75.2）；④ push 后盯 CI；⑤ 确认 Dependabot 告警自动关闭。
- **完结（a9c0ff7，2.75.2）**：干净重装后 bash 里测试仍 155 全挂 ⇒ 根因不是 node_modules，是 **vitest#10692 Windows 小写盘符 bug**：Git Bash cwd 是小写 `f:`，vitest 运行时被 ESM 解析成两份模块副本，首个 `describe()` 抛 `reading 'config'`（4.1.10/4.1.11 都会中招）；PowerShell 会大写盘符所以没事——上一条「vitest 一律用 PowerShell」的真正原因就是它。PowerShell 下 `npm run check` 全绿（tsc + eslint + vitest 全量），提交推送 CI 通过，Pages 已部署，Dependabot 开放告警清零。
- gh 小坑：`gh run watch` 非 TTY 必须传 run ID；`gh run list --commit <short-sha>` 查不到，要用完整 SHA。

## 精选路线批量接入：工具链修复 + 上海首批（[NF] fad47f1）

- 需求：全国各地热门路线「每地区 20 条」= 10 个新城市 + 存量 6 地区补齐 ≈ 300 条。
- **curate-route.mjs 实测能跑通，但有 5 处缺陷（已全部修掉）**：
  ① `fetchWaysMirrors` 缺 User-Agent → overpass-api.de 406、private.coffee/kumi 429，三镜像全挂只剩 curl 兜底（每次白等三轮）；加 UA 后首个镜像直连成功。
  ② types.ts `CuratedRegionId` 插入缺换行 → 拼成 `| 'kunming'  | 'shanghai'` 单行。
  ③ 选路失败路径未 mkdir 就写 report.txt → ENOENT 崩溃，诊断报告也丢了。
  ④ 新地区 `REGION_BBOX` 行被插到对象闭合 `}` **之后** → 测试文件语法错误（tsc 直接红）。
  ⑤ Photon 查询拼入 `@提示词` 反而无结果（「佘山地铁站 上海松江」查不到，单查原名 + 中心 bias 正常）。
- 新增 `scripts/curate-batch.mjs`：spec 数组批量驱动，`--only/--from/--dry/--force/--delay`，失败不中断并汇总；支持文件级 `defaults`（新地区 regionLabel/entryPrefix 只写一次）。
- 新增 SRTM 90m 爬升估算（需求单 `elevEstimate: true`）：沿几何自适应重采样 + 3m 噪声阈值 + 坐标级缓存；梧桐山实测 521m vs 野途权威 533m，**误差 2.3%**。
- **数据可得性硬约束（后续城市排期必读）**：官方/媒体只发布**里程**，不发布路书或 GPX → 几何只能靠锚点在 OSM 上选路，走向是推断的，必须看 `.tmp/curate/<id>/preview.html` 人工审核；多数几何只有申报里程的 0.5–0.8，只能标 `geometryScope: 'core'`。平原城市（上海/海南）没有权威爬升来源，靠 SRTM 估算。**上海实际只凑到 6 条，不是 20 条**——20 条/地区在这个数据标准下不现实，需按实际可得性分批交付。
- 本提交未动 package.json / changelog：并行会话的 vitest 4.1.11 升级仍在工作区，改动它会卷入他人未提交内容，版本号顺延给该会话。
- 下次继续：按 `.tmp/curated-national/shanghai-specs.json` 的格式做下一个城市（南京/厦门/广州/海南/武汉/重庆/大理/青岛/青海湖），每个城市一批，跑完 `npx tsc -b` + curatedRoutes/routes 测试再提交。

## 精选路线全国铺开（[NF] 6dd6ac2，13 地区 109 条）

- **⚠️ 重大事故：跨行正则删条目误删 97 条**。用 `/  \{\n[\s\S]*?id: 'xxx'[\s\S]*?\n  \},\n/` 删除数据条目，
  `[\s\S]*?` 会跨越整个对象（甚至跨条目）匹配，`replace` 后 national.ts 从 104 条掉到 7 条。
  恢复靠两条：几何数据在 `nationalTracks.ts` 里完好（没动它）；条目文本在 `.tmp/curate/<id>/proposed-entry.ts`
  有落盘 → 写 `rebuild-entries.mjs` 按行定位重建。
  **教训：改这类生成的大数据文件一律按行定位（`findIndex` + 向上找 `{`、向下找 `},`），禁止跨行贪婪正则。**
- **curate-route.mjs 又暴露 2 处 bug（累计 7 处）**：
  ⑥ `entryId = prefix + '-' + spec.id`，而需求单 id 往往已带前缀 → 生成 `nj-nj-laoshan`；
  ⑦ `tracks: NATIONAL_TRACKS.${spec.id}`，id 含连字符时是非法 JS（`NATIONAL_TRACKS.nj-laoshan`）
  → 必须判标识符合法性，非法改 `NATIONAL_TRACKS['nj-laoshan']`。
- **性能瓶颈（关键）**：`generate-curated-routes.mjs` 的 Dijkstra 是朴素 O(V²)（每次扫全表找最小），
  30km+ 路线 bbox 有数万节点 → 南京紫金山环陵路卡死 11 分钟无响应。换二叉最小堆后 O(E log V)，
  同一条 19 秒返回。这是长距离路线能否批量产出的分水岭。
- **Overpass 并发红线**：同时跑 6–8 个城市会被限流（fetch failed / 全镜像 429），
  症状是「几十分钟没有任何新目录产生」。有效并发只有 2–3；已启动的多余进程要主动停掉让路。
- **snapNode 半径 800m 对山区太紧**：崂山/青海湖这类 OSM 测绘稀疏的区域 7 个锚点全吸附失败，
  放宽到 1200m 后可解（起点最多偏 1.2km，对几十公里示意几何可接受）。
- **数据准入标准**（写进 `filter-quality.mjs`）：几何长度/申报里程 <0.3 或 >1.6 不入库；
  点数 <12 不入库；本轮据此剔除 12 条（多为 OSM 只测绘了绿道一段，或选路绕远）。
- **测试断言同步**：`curatedRoutes.test.ts` 有三处硬编码（NATIONAL_TRACKS key 全集 / 各地区界框 /
  注册表顺序）。写 `sync-test.mjs` 从数据自动重生成，避免每次手改。
  **坑：tracks key 与条目 id 不一定相同**（原始条目 `id:'xa-fenshuiling'` 指向 `tracks.fxl`），
  算地区界框必须按条目块内的 tracks 引用反查 region，否则漏算该条坐标导致界框断言误挂。
- `routesMapPage.test.tsx` 硬编码「西安 · 1」改动态取数；条目涨到 109 条后渲染变慢，
  两条用例超时 5s → 放宽到 20s。
- 结果：上海 6/深圳 14/广州 11/杭州 9/昆明 9/成都 8/西安 8/重庆 7/厦门 7/海南 7/武汉 9/台州 6/南京 4/大理 4。
  版本号仍未动（package.json 与 changelogData.ts 被并行会话的 vitest 升级占用）。

## 20:40 批次 2 收尾：青岛/青海湖接入 + 孤儿几何清理（cbb942d）

- 429 限流中断的 drop-entry ×4（hn-boao-wanning/km-xishan-climb/nj-shijiu/qh-xihai-hudong）
  实际已完成；终态 123 条/16 地区。自写 `final-validate.mjs` 全量复核：tracks 引用 0 缺失、
  id 无重复、几何/标称比值（full 0.3~1.6、core ≥500m）逐条通过。
- 清理 11 个孤儿 tracks（条目被质量过滤剔除但几何残留）：按括号配对逐 key 删除，
  正则跨条目匹配不可靠（97 条误删事故教训），复验 keys=refs=123。
- **vitest Windows 小写盘符 bug（重要，能省半小时排查）**：cwd 是 `f:`（小写）时，
  测试文件 import 的 vitest 会解析成第二份运行时 → 全部文件报
  "Cannot read properties of undefined (reading 'config')" / "failed to find the runner"。
  上游 issue #10692。解法：用大写盘符 `cd F:/...` 起跑。154/155 全挂别急着怀疑依赖坏了。
- 提交 cbb942d 并统一 push（含批次 1 的 6dd6ac2），CI 双 run 盯到绿。
  版本号继续顺延（package.json/changelogData.ts 仍被并行会话占用）。
- `.tmp/curated-national/`（specs/验证脚本）与 `.tmp/curate/<id>/`（proposed-entry/geometry
  缓存）保留：PROGRESS.md 待办「各地区补到 20 条」还要用，删除会丢需求单与 Overpass 成果。
