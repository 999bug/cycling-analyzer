# 2026-09-01 工作日志

## 全面代码审计（只读，未改源码）

对 cycling-analyzer 做了 4 路并行深度审查 + grep/构建/测试实测复现，结论写入 `docs/代码审计报告-2026-09-01.md`。

### 最关键的发现
- **P0 崩溃**：`fit/calculator/calculator.ts:174` `Math.max(...valid)` 展开超栈（>6.5万点 RangeError）；赛段页 `SegmentsPage.tsx:116-123` N+1 + 每赛段全量 postMessage 克隆风暴；无 ErrorBoundary + PWA 未注册 onNeedRefresh → 发版白屏；解析 Worker 不 terminate/无超时 → 导入卡死；`db.ts` 无 versionchange 监听 + 无 QuotaExceededError 处理。
- **P1 真 bug 嫌疑**：时区口径分裂（repository 用 UTC `startsWith`，calendar/statistics 用本地 `localDateKey`），且全量测试恰有 2 个失败用例都涉跨月/日期范围，**今天 2026-09-01 正好跨月**——极可能是同一根因，优先排查。
- **结构**：架构边界失守（5 处 UI `new` 仓库 / import db；`parseWorker.ts:8` 反向依赖 features），ESLint 无 `no-restricted-imports` 兜底；重复代码（LoadState×6、localDateKey×3、formatDuration 系×6、METERS_PER_MILE 硬编码绕过）；SettingsPage 718 行单组件 / ActivityDetailPage 1008 行。

### 验证数据
- 构建产物：JS 总 ~1.5MB；`recharts` 412kB + `dataSourceStore` 103kB 被 `index.html` modulepreload（首屏过重），根因是 DashboardPage/AppLayout 静态链。
- 测试基线：967 用例，965 通过 / 2 失败（均为日期相关）。

审计为只读，未提交。如需落地修复，建议按报告 §5 路线图：先 P0 崩溃项 → 时区真 bug → 坐标 ?? 0 → 赛段批量查询 → 详情页 buildClimbs memo。
